on()entiPrevDuplicate
}

testxit(1)
  }  process.e
  sage)rr.mesn:', ee preventiong duplicatstiError teror('❌ er console.) {
    catch (err   
  }ed!')
 mplett coesvention ticate pre('\n🎉 Dupl.lognsole 
    co  })
         }
  `)
on}tiate.descripplemog(`     ${tsole.l       con) {
 ontiate.descrip (templ`)
      ifpe}tye_timlate.emp${trate}/ay_late.base_pemp}: $${tsplay_nametemplate.dig(`   - ${  console.lo=> {
    e h(templatlc?.forEaclT fina   
   lse })
 ending: faate', { ascse_pay_rer('ba.ord     rue)
 ve', teq('is_acti   .nator')
   tics_coordialent_logis', 't'role  .eq(
    roject.id)t_id', testPecojeq('pr
      .n')descriptioe_type, _rate, timase_payay_name, bt('displ     .selecmplates')
 ect_role_te .from('projbase
     wait supaTlc } = afinal{ data: st   cones:')
  lattempnal TLC \n🔍 Fiog('ole.l
    consl state Show fina   //
   
  }
    }   
   `)type}reated.time_/${cy_rate}ted.base_pa $${crea} -splay_namedi ${created.eated:ly cr Successfullog(`✅onsole.        cse {
  } elge)
    .messaErrortecreaad TLC:', ing Lecreatr rro'❌ Eole.error(   cons {
     eateError)crif (
         gle()
           .sinselect()

        .ate)pliqueTemert(unins     .   tes')
le_templarot_om('projec       .frbase
 pait su = awarror }ror: createEreated, er data: c    const {  
else {)
    } on'ng creatikippixists, slready ete a" templa TLC'⚠️  "Lead.log(     console) {
 ead (existingL    
    if.single()
)
      ead TLC'me', 'L'display_na.eq(  )
    ator'rdintics_cooent_logis 'talq('role',.e     t.id)
 ec, testProjproject_id'('     .eqt('id')
     .selecplates')
  emct_role_t.from('projebase
      upa await s =ad }gLexistin { data: est    conts first
isady exit alreCheck if    // 
  }
    '
   ibilitiessponsment reageitional mand with addm leaption: 'Teadescri  ily',
    e: 'da    time_typ00,
  450.e_pay_rate: bas
      TLC',ad e: 'Leamplay_n  distor',
    cs_coordinaistiog_lent: 'talrole     .id,
 ecttestProjct_id:  proje
     = {e queTemplatconst uni
    ate...')templead TLC" "Lnique  Creating ut 2:Tesog('\n🔍 le.lnso)
    could succeedemplate (sho unique teweate a n Cr 2:/ Test    /   }
    
xpected')
  is unehised - t preventas notte wg('❌ Duplicaole.lo  cons    se {

    } ele)messagteError. : duplicaing'int worktraabase cons') ? 'Datraintstunique conludes('ince.sagror.mesteErca', dupli prevented:correctlye uplicatle.log('✅ Dconso      
cateError) {  if (dupli   
  ()
 ngle    .si)
   .select(
     e)ateTemplatlicert(dup    .ins
  mplates')ect_role_tem('proj  .froabase
    up } = await sErrorate: duplic errorte,: duplica datat { cons
    
   '
    }ailyype: 'dme_t   ti25.00,
   e: 4ase_pay_rat  b,
    nior TLC'name: 'Se display_or',
     rdinats_coont_logisticle: 'tale   rot.id,
   rojecestP: tect_id  proj
    emplate = {plicateTdu const    TLC"...')
nior ate "Seliccreate dupo g ttin 1: Attempn🔍 Test'\.log(
    consoleuld fail)shoe (licatdupo create a Try t/ Test 1:    
    / })
 e}`)
   ime_typate.t${templ_rate}/.base_paytelatemp_name}: $${e.display- ${template.log(`     consol  e => {
  Each(templat.foringTlc  exist
          }
n
   returessage)
   ror.mfetchErmplates:', TLC ter fetching Errole.error('❌   consor) {
    hErroetcif (f
     })
    g: falsendince', { asy_rate_paorder('base
      . true)s_active',   .eq('i
   or')dinatoor_logistics_ce', 'talent.eq('rol    t.id)
  testProjecd', _iojecteq('pre')
      .me_typ tise_pay_rate,y_name, badispla('ect
      .sel')tesle_templarooject_  .from('prbase
    ait supa } = aw fetchError error:xistingTlc,{ data: e    const )
s:' templateCurrent TLCog('\n🔍 onsole.l c
   LC role for Tplatesexisting temhow / S 
    /.id})`)
   tProject{tesme} ($tProject.na${tesproject: h esting witle.log(`📋 T]
    conso projects[0 =tProjectes  const t    }
    
 return
       est with')
found to tts  No projec('❌sole.error con     0) {
 ===.length | projects | (!projects   
    if   }
 
 rn
      retussage)ror.meojectsErjects:', prg proinr fetchroror('❌ Ernsole.er{
      coctsError)    if (proje
     1)
it(.lim
      'id, name')t(      .selecprojects')
    .from('upabase
  ait s = awError }cts: projeror, erta: projects  const { da with
  testt to ecGet a proj   //   try {
 
...')
  entsn improveme preventioplicatesting du Tole.log('🧪ons {
  crevention()tePplicaon testDunc functi
asyey)
iceKabaseServbaseUrl, supent(supaCli createbase =const supa}


xit(1)ocess.e_KEY')
  prVICE_ROLESUPABASE_SER and UPABASE_URLXT_PUBLIC_SNEeed: e.error('Nonsolocal')
  cn .env.ln iatioconfigurpabase sing Suor('❌ Mis.erroley) {
  consrviceKeeSe| !supabasl |!supabaseUr
if (_ROLE_KEY
_SERVICESUPABASE= env.Key seServicepabaonst suE_URL
cIC_SUPABASPUBL = env.NEXT_Urlnst supabase }
})

coim()
 in('=').trjoalueParts.)] = vey.trim({
    env[kh > 0) ngteParts.le && valu)
  if (key'='.split(rts] = line ...valuePat [key, {
  cons=>Each(line ').fort('\ntent.splivCon {}
en
const env =esriablironment vase env/ Par

/f8')th, 'utnvPaeSync(e fs.readFil =ntentconst envCo.local')
.', '.env, '.n(__dirnamejoih = path. envPatconst
 .env.localbles fromiament vard environ Loa')

//uire('pathth = req
const pae('fs')uirfs = reqs')
const supabase-jpabase/equire('@sulient } = r { createC

constlogic
 */n e preventioatplicved due impros thtestript  This scon
 *ntiicate Preveupl Test De

/**
 *bin/env nod#!/usr/